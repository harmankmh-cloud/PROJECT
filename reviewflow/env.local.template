# Copy to .env.local and fill in your values:
#   cp env.local.template .env.local

# RateLocal project: reviewflow (otnddwopphhxstteqizw) — ca-central-1
NEXT_PUBLIC_SUPABASE_URL=https://otnddwopphhxstteqizw.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

OPENROUTER_API_KEY=sk-or-v1-your-key
OPENROUTER_MODEL=google/gemini-2.0-flash-001

NEXT_PUBLIC_APP_URL=http://localhost:3000

# Platform owner email (same as login) — opens /admin panel
ADMIN_EMAILS=you@gmail.com

# Supabase Auth → URL Configuration → add these Redirect URLs:
# http://localhost:3000/auth/callback
# http://localhost:3000/auth/callback?next=/onboarding
# https://project-amber-omega-94.vercel.app/auth/callback
# https://ratelocal.ca/auth/callback
# https://ratelocal.ca/auth/callback?next=/onboarding
#
# Google OAuth (optional — shows "Continue with Google" on login/signup):
# 1. Supabase → Authentication → Providers → Google → Enable
# 2. Google Cloud Console → OAuth client → Authorized redirect URI:
#    https://otnddwopphhxstteqizw.supabase.co/auth/v1/callback
# 3. Set NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=true below
NEXT_PUBLIC_GOOGLE_AUTH_ENABLED=false

# Stripe — https://dashboard.stripe.com (use Test mode first)
STRIPE_SECRET_KEY=sk_test_
STRIPE_PRICE_MONTHLY=price_1TfmkXDwgNgi4Q9Vl48M1TSE
STRIPE_WEBHOOK_SECRET=whsec_

# Datadog RUM (optional — error + session monitoring on ratelocal.ca)
# Datadog → UX Monitoring → RUM Applications → New application → Browser
NEXT_PUBLIC_DATADOG_APPLICATION_ID=
NEXT_PUBLIC_DATADOG_CLIENT_TOKEN=
NEXT_PUBLIC_DATADOG_SITE=datadoghq.com

# CallLocal — Twilio missed-call SMS (https://www.twilio.com/console)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Activepieces — new review notification flow (optional)
# Copy webhook URL from Activepieces → RateLocal - New Review Notification → trigger
ACTIVEPIECES_REVIEW_WEBHOOK_URL=
# Must match header auth on the flow trigger (no default — set in Vercel)
ACTIVEPIECES_WEBHOOK_SECRET=
