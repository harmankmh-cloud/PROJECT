/**
 * Shim — the site chrome was unified on the landing system.
 * All pages importing MarketingFooterNew now render the canonical LandingFooter
 * (rich 4-column link grid + global TrustBar).
 */
export { LandingFooter as MarketingFooterNew } from "@/components/landing/LandingFooter";
