# Monorepo environment variables
#
# RateLocal (reviewflow): see reviewflow/env.local.template — Supabase, Stripe, OpenRouter, Resend/SMTP, admin emails
# ServeLocal (servelocal): see servelocal/.env.local.template — Supabase, Stripe, Resend, cron secret
#
# Google Stitch (design generation CLI)
# Get a key from https://stitch.withgoogle.com/ → Settings → API Keys
STITCH_API_KEY=
